function App() {
  return (
    <main className="app-shell">
      <section className="app-card" aria-labelledby="app-title">
        <p className="eyebrow">Issue #1</p>
        <h1 id="app-title">Vite + React + TypeScript initialized</h1>
        <p className="description">
          The project scaffold is in place and ready for the clock features,
          styling, and containerization work in the remaining issues.
        </p>
      </section>
    </main>
  )
}

export default App
